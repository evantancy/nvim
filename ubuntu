#!/usr/bin/env bash

# environment variables
if [[ -z $STOW_FOLDERS ]]; then
	STOW_FOLDERS=("nvim" "tmux" "i3" "dunst" "bash" "zsh" "git")
fi

if [[ -z $DOTFILES ]]; then
	DOTFILES=$HOME/.dotfiles
fi

STOW_FOLDERS=$STOW_FOLDERS
DOTFILES=$DOTFILES
